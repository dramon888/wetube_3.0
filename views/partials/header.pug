header.header
    .header__column
        h1.header__title #{siteName}
        a(href=routes.home)
            i.fab.fa-youtube
    .header__column
        ul
            li
                a(href=routes.join) Join
            li
                a(href=routes.login) Login
        