header.header
    .header__column
        h1.header__title #{siteName}
        a(href=routes.home)
            i.fab.fa-youtube
    .header__column
        form(action=routes.search, method="get")
            input(type="text", placeholder = '찾고자 하는 video Name 검색해주세요.', name='term')
    .header__column
        ul
            li
                a(href=routes.join) Join
            li
                a(href=routes.login) Login
        